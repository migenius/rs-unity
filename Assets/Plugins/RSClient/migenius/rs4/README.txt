This folder contains a C# port of the JavaScript client library as of RS 4.2
Everything in this folder except the code in the 'unity' folder should be standard
C# code that could be used anywhere and not just in Unity.
